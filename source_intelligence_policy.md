# Source intelligence persistence policy

Discovery may inspect thematic Telegram references during a run, but speculative candidates are ephemeral.
Only Telegram sources where at least one direct BetBoom wheel link was actually observed are persisted to `intelligence_state.json` and exposed for administrator review.
