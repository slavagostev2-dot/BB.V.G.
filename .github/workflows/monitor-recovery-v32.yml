name: BB V.G. monitor recovery v32 (archived)
run-name: Archived legacy recovery

on:
  workflow_dispatch:

permissions: {}

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived. Recovery is handled by monitor.yml and monitor-watchdog.yml."
