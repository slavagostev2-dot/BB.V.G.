name: BB V.G. live 66-source monitor (archived)
run-name: Archived legacy monitor

on:
  workflow_dispatch:

permissions: {}

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived. Production uses monitor.yml only."
