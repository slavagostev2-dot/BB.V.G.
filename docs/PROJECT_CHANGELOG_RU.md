# BB V.G. — журнал значимых изменений проекта

Этот документ читается после `AGENTS.md`. Он фиксирует изменения поведения, архитектуры, структуры, workflow, состояния и эксплуатации. Новые записи добавляются сверху.

## Шаблон записи

```text
## YYYY-MM-DD — Краткое название

Причина:
Что изменено:
Затронутые модули и файлы:
Проверки:
Откат:
```

Не добавляйте сюда автоматические heartbeat-коммиты, обычные сохранения runtime JSON и форматирование без изменения смысла.

---

## 2026-07-16 — Объединены последние слои Telegram runtime

**Причина:** функции сводки, очистки декоративных цветных меток и главного экрана были распределены по `admin_panel_runtime_v39.py`, `v40.py` и `v41.py`, увеличивая цепочку наследования и усложняя дальнейшие изменения.

**Что изменено:**

- создан постоянный модуль `bbvg/bot/runtime.py` без номера версии;
- в нём объединено фактическое поведение v39–v41 поверх стабильной v38;
- `admin_panel_runtime_v39.py`, `v40.py` и `v41.py` превращены в короткие совместимые обёртки;
- создана автоматически генерируемая карта `docs/RUNTIME_METHOD_INVENTORY_RU.md`;
- runtime-аудит расширен списком классов, методов, переопределений и объёмом кода;
- рабочая цепочка исторических runtime сократилась с 38 до 36 файлов;
- суммарный объём исторических runtime уменьшился с 10 875 до 10 564 строк.

**Затронутые файлы:** `bbvg/bot/runtime.py`, `admin_panel_runtime_v39.py`, `admin_panel_runtime_v40.py`, `admin_panel_runtime_v41.py`, `tools/cleanup_audit.py`, `.github/workflows/cleanup-audit.yml`, `.github/workflows/refactor-validation.yml`, `docs/RUNTIME_METHOD_INVENTORY_RU.md`.

**Проверки:** компиляция нового runtime; self-test сводки, главного меню и очистки цветных меток; проверка callback до 64 байт; сравнение вывода активных колёс с прежней v41; полный объединённый acceptance-набор. Последний подтверждённый run: `29471743216`.

**Откат:** изменения находятся только в ветке `refactor/consolidate-runtime-v42`; production `main` не переключён. Полный откат — `backup/v41-before-cleanup-2026-07-16`.

## 2026-07-16 — Начато объединение производственных acceptance-проверок

**Причина:** пять корневых файлов `chapter1_stability.py` — `chapter5_acceptance.py` выполняют одну ответственность, усложняют навигацию и требуют дублирующих ссылок в workflow и preflight.

**Что изменено:**

- добавлен единый `tests/production_acceptance.py`;
- проверки разделены на именованные секции `stability`, `unified`, `ci`, `interface`, `lifecycle`;
- добавлена карта `docs/CODE_INVENTORY_RU.md` с фактическим графом runtime и порядком переноса;
- добавлен временный workflow параллельной проверки старой и новой acceptance-схемы;
- старые `chapter*` пока не удалены и используются как эталон эквивалентности.

**Затронутые файлы:** `tests/production_acceptance.py`, `docs/CODE_INVENTORY_RU.md`, `.github/workflows/refactor-validation.yml`, этот журнал.

**Проверки:** компиляция нового файла; последовательный запуск пяти старых acceptance-файлов; запуск каждой новой секции отдельно и всего набора целиком.

**Откат:** удалить новые файлы из ветки рефакторинга; production `main` не изменён. Полный откат — `backup/v41-before-cleanup-2026-07-16`.

## 2026-07-16 — Введены архитектурные правила и обязательный журнал

**Причина:** проект разросся из-за цепочки файлов с номерами версий, дублирующих проверок и отсутствия единой карты для новых чатов.

**Что изменено:**

- в корне добавлен `AGENTS.md` как обязательная инструкция перед работой;
- установлен приоритет изменения существующих модулей вместо создания новых файлов;
- новый файл разрешён только при отдельном архитектурном обосновании;
- зафиксирована целевая структура `bbvg/bot`, `bbvg/monitor`, `bbvg/domain`, `bbvg/storage`, `bbvg/operations`;
- создан этот журнал и введено требование обновлять его при каждом значимом изменении;
- создана резервная ветка `backup/v41-before-cleanup-2026-07-16`;
- архитектурный рефакторинг ведётся в `refactor/consolidate-runtime-v42`.

**Затронутые файлы:** `AGENTS.md`, `docs/PROJECT_CHANGELOG_RU.md`, документы рефакторинга в `docs/`.

**Проверки:** изменения документационные и не меняют production-код, Telegram callback, состояние или workflow.

**Откат:** удалить документационные коммиты либо вернуть `main` к резервной ветке.

## 2026-07-16 — Упрощён интерфейс активных колёс

**Причина:** разноцветные маркеры на каждом колесе и каждой кнопке создавали визуальную «радугу» и ухудшали ориентацию.

**Что изменено:** декоративные цветные кружки удалены; связь карточки и действий строится по номеру колеса; callback-данные не изменены.

**Затронутые модули:** интерфейс Telegram-панели v40/v41 до завершения консолидации runtime.

**Проверки:** проверка разметки Telegram, лимита callback 64 байта и сохранения исходных callback/URL.

**Откат:** резервная ветка v41 до архитектурной чистки.

## 2026-07-16 — Введён жизненный цикл повторного использования ссылки колеса

**Причина:** одинаковая ссылка могла повторно прийти ночью, после перезапуска или из другого Telegram-источника и ошибочно считаться новым колесом.

**Что изменено:** автоматический таймер блокирует ссылку до прокрутки; ручной таймер имеет приоритет; без таймера действует окно 2 часа; после окончания окна ссылка может создать новое событие; новое событие не наследует участие и публикации старого.

**Затронутые модули:** `wheel_link_lifecycle.py`, монитор, административная установка времени, сценарные тесты.

**Проверки:** одинаковый пост, пересылка, другой канал, ночной перезапуск, ручной таймер, окно 2 часа и повторное использование ссылки.

**Откат:** резервная ветка v41 до архитектурной чистки.

## 2026-07-16 — Исправлены сводка и начисление рейтинга при завершении

**Причина:** сводка зависела от запуска второго workflow, а завершение колеса требовало идемпотентного начисления рейтинга.

**Что изменено:** сводка формируется внутри панели; действие «Завершено» начисляет рейтинг источникам один раз и не дублирует очки.

**Затронутые модули:** панель v39–v41 до консолидации, `admin_action_v3.py`, `rating_policy.py`.

**Проверки:** self-test сводки, завершения, повторного действия и рейтинга нескольких источников.

**Откат:** резервная ветка v41 до архитектурной чистки.
